mixin form(action, method, fields)
    form(action=action method=method)
        -for(field of fields)
            .form-group
                label=field.label
                input(name=field.name type=(field.type || 'text') value=(field.value || ''))
                div.error-msg=field.errorMsg 
        button#submit submit